# Metal Tracker - Environment Konfiguration
# Kopiere diese Datei nach .env und passe die Werte an

# Debug-Modus (aktiviert API Docs unter /docs)
DEBUG=false

# Erlaubte Origins fuer CORS (komma-getrennt)
ALLOWED_ORIGINS=https://metal-tracker-tn-ffb450a69489.herokuapp.com

# Frontend URL (fuer OAuth Redirects)
FRONTEND_URL=https://metal-tracker-tn-ffb450a69489.herokuapp.com

# Datenbank URL (wird von Heroku automatisch gesetzt)
# DATABASE_URL=postgresql://user:pass@host:5432/db

# === JWT KONFIGURATION ===
# Geheimer Schluessel fuer JWT Token (generiere einen sicheren Key!)
# python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=CHANGE-ME-IN-PRODUCTION-use-secrets-token

# JWT Algorithmus (Standard: HS256)
JWT_ALGORITHM=HS256

# Token-Gueltigkeitsdauer in Minuten (Standard: 60)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Session Secret (fuer OAuth State)
SESSION_SECRET=CHANGE-ME-IN-PRODUCTION-session-secret

# === GOOGLE OAUTH (optional) ===
# Erstelle OAuth Credentials unter: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://metal-tracker-tn-ffb450a69489.herokuapp.com/api/auth/google/callback

# === MIGRATION (einmalig) ===
# Fuer die Migration bestehender Daten zu einem Admin-User
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=sicheres-admin-passwort
